<?xml version="1.0" encoding="UTF-8"?>
<solver>
    <!-- Modelo de domínio -->
    <solutionClass>AlocacaoHorario</solutionClass>
    <entityClass>domain.Alocacao</entityClass>

    <!-- Score -->
    <scoreDirectorFactory>
        <scoreDefinitionType>HARD_MEDIUM_SOFT</scoreDefinitionType>
        <scoreDrl>alocacaoHorarioScoreRules.drl</scoreDrl>
        <!-- <easyScoreCalculatorClass>br.com.devmedia.agenciaturismo.planejamento.score.AlocacaoPassageirosScoreCalculator</easyScoreCalculatorClass> -->
        <initializingScoreTrend>ONLY_DOWN</initializingScoreTrend>
    </scoreDirectorFactory>

    <!-- Algoritmos de otimização -->
    <exhaustiveSearch>
        <exhaustiveSearchType>BRUTE_FORCE</exhaustiveSearchType>
    </exhaustiveSearch>
</solver>